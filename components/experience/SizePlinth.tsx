import { useState } from "react";
import { product } from "../../data/product";
import { useExperienceStore } from "../../store/experienceStore";
export function SizePlinth(){ const {selectedSize,setSelectedSize}=useExperienceStore(); const [guide,setGuide]=useState(false); return <div className="sizePlinth"><div className="plinthLine" /> <div className="sizes" aria-label="Available sizes">{product.availableSizes.map(size=>{ const unavailable=product.unavailableSizes.includes(size); return <button key={size} disabled={unavailable} aria-label={unavailable?`Size ${size} unavailable`:`Select size ${size}`} className={`${selectedSize===size?"selected":""} ${unavailable?"unavailable":""}`} onClick={()=>!unavailable&&setSelectedSize(size)}>{size}</button>})}</div><button className="sizeGuide" onClick={()=>setGuide(!guide)}>SIZE GUIDE</button>{selectedSize&&<p className="selectedSize">FORM {selectedSize} SELECTED</p>}{guide&&<div className="ruler"><span>24cm</span><span>26cm</span><span>28cm</span><span>30cm</span></div>}</div>}
